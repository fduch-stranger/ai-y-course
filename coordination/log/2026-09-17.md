# 2026-09-17

## Course-alignment audit

- Compared the repository against the original starter prompt and pinned upstream revisions rather than treating the prior base smoke test as full course acceptance.
- Restored the missing `~/tz-skills` checkout at pinned revision `8fbe187c316e788fcf5d9da36bd0ebf0c19f79c5`; confirmed `~/parallel-ai-dev` is at pinned revision `706aae5fc19c7a023b62e4e343468c76f2373e56`, version 2.0.2.
- Confirmed the four project-local skill contracts and NIM model choices match the pinned course source; retained documented Codex substitutions for Claude-specific paths and commands.
- Corrected stale statements claiming that no remote existed after verifying local `main`, `origin/main`, and the remote ref matched.
- Restored the course acceptance distinction: base Codex work remains available without NVIDIA, but the bootstrap reports red until the required three independent critic slots pass.
- Added `coordination/COURSE_ALIGNMENT.md` as the canonical requirement map. Remaining owner action: provide `NVIDIA_API_KEY`; never commit or print it.

## Functional smoke test

- Existing work: this is a readiness check of the installed starter kit and four project-local TZ skills; no product implementation exists yet.
- `bash scripts/check-setup.sh` passed R1–R9. Skill metadata validation found all four expected skills and all three configured critic slots.
- A fresh `serena project health-check` used the project-local LSP backend, started Bash, Python, and TypeScript servers, and passed symbol overview, symbol lookup, reference lookup, and pattern search against a repository shell script. Python and Bash emitted startup timeout warnings but Serena completed successfully.
- `bash scripts/llm-critic.sh --smoke critic_a` returned PONG. A read-only Codex probe loaded all four TZ skills: tz-draft produced a structured JTBD; tz-review, tz-verify, and tz-go completed their bounded preflight/dry-run paths and emitted all four expected markers.
- Full three-vendor tz-review/tz-verify remains unavailable because `NVIDIA_API_KEY` is absent. This is the documented optional configuration; tz-go can fall back to ordinary Codex review and local checks while disclosing skipped critic stages.
- The full bootstrap reproduced a bug: it returned red when the external IMMUNE source file was absent even though AGENTS.md already contained the block. Reordered the checks so satisfied project state wins, and recorded the mechanism lesson in MISTAKES.md.
- Serena health checks generate `.serena/logs/`; added the directory to Serena's local ignore file so diagnostics do not dirty Git.
- Verification: the complete bootstrap exited 0 from a clean committed state. It preserved all four skills and providers.json, recognized the existing IMMUNE block, passed R1–R9, and received PONG from critic A. Follow-up setup, diff, and clean-tree checks run after recording this result.

## Additional Serena languages

- Scope: add Python and TypeScript to the existing Bash LSP configuration at the owner's request. Validate with Serena's configuration loader and the project setup check.
- Added `python` and `typescript` to `.serena/project.yml`; updated the existing backend decision. TypeScript covers JavaScript as well.
- Live language server startup remains unverified until Serena restarts with this project selected.

## Serena project backend

- Scope: set a project-only LSP override and enable Bash for existing scripts; keep global settings unchanged. Verify using Serena's configuration loader and the project setup check.
- Set `.serena/project.yml` to `language_backend: LSP` and `languages: [bash]`; recorded the reason in DECISIONS.md.
- The active MCP session reported JetBrains before this edit. Backend selection is fixed at startup, so the existing connection needs to restart with this project selected before it can expose LSP tools.
- Verified with the installed Serena configuration loader: the project selects LSP with Bash even when the global default is JetBrains. `git diff --check` passed. The existing Codex MCP launch arguments include `--project-from-cwd`, so no global launch configuration change is needed.
- Next action: restart the Serena connection in this project to apply the new backend. Live Bash LSP startup was not exercised in the existing JetBrains session.

## Readiness audit

- Existing starter setup confirmed; no application code or deployment was exercised.
- Activated the project in Serena and read its core memory and canonical coordination records.
- All four project skills are present with name/description metadata and exposed in the current Codex session: tz-draft, tz-go, tz-review, tz-verify. Full workflows were not executed during this audit.
- `bash scripts/check-setup.sh` exited 0: local files, shell syntax, provider JSON, and R1–R9 checks passed. The checker reported an unknown upstream kit version (`v?`).
- `bash scripts/llm-critic.sh --smoke critic_a` initially exited 2 because the outer sandbox blocked Codex state writes and initialization. Retrying with approved elevated access exited 0 and reported `slot 'critic_a' OK` after PONG.
- The successful smoke run also emitted nonblocking user-level warnings for plugin icon paths, the unavailable `idea` MCP endpoint, and shell snapshot validation. These integrations are not verified by PONG.
- NVIDIA_API_KEY is absent; full three-vendor tz-review/tz-verify remains unavailable. tz-go may use ordinary Codex review and local checks as authorized in AGENTS.md.
- Local Git was clean at the start. An origin remote is configured, contradicting historical project notes; no remote contact or push was performed.
- Changed only this handoff and added audit follow-ups to BACKLOG.md. No credentials or runtime configuration changed.
- Next action: the owner can start drafting or implementation now. A future maintenance task can reconcile remote documentation and investigate CLI warnings; optional critic setup remains deferred.
