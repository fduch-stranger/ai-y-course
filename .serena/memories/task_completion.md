# Completion

Run `bash scripts/check-setup.sh`; the upstream memory check requires memory changes to be committed. Before committing, validate shell syntax and `git diff --check`; after committing rerun the setup check. Record deferred work in coordination/BACKLOG.md and a handoff in coordination/log/. Push only if an authorized remote exists. Optional critic absence must be disclosed separately from local setup readiness.
