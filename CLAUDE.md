# Compatibility pointer — this project uses Codex

The canonical project instructions, project zones, and IMMUNE rules are in `AGENTS.md`. Read that file. Claude is not required.

This file remains because the original parallel-ai-dev memory checker expects a file named CLAUDE.md. It owns no independent rules. Working memory lives in `coordination/DECISIONS.md`, `coordination/PROJECT_MAP.md`, and `coordination/BACKLOG.md`.

`CLAUDE.parallel-ai-dev.md` is preserved upstream reference material only.
